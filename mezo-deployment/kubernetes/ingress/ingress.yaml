apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mezo-ingress
spec:
  rules:
  - host: mezo.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mezo-frontend-service
            port:
              number: 80
