{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default read-only capability for the MEZO remote client",
  "windows": ["main"],
  "permissions": ["core:default"]
}
