{
  "name": "mezo-ai",
  "private": true,
  "workspaces": [
    "mezo-frontend",
    "mezo-desktop"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=mezo-frontend",
    "dev:desktop": "npm run dev --workspace=mezo-desktop",
    "build:web": "npm run build --workspace=mezo-frontend",
    "build:desktop": "npm run build --workspace=mezo-desktop"
  }
}
