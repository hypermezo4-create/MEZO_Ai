{
  "name": "mezo-ai",
  "private": true,
  "workspaces": [
    "mezo-frontend",
    "mezo-desktop"
  ],
  "scripts": {
    "dev:web": "npm --prefix mezo-frontend run dev",
    "dev:desktop": "npm run dev --workspace=mezo-desktop",
    "build:web": "npm --prefix mezo-frontend run build",
    "build:desktop": "npm run build --workspace=mezo-desktop"
  }
}
